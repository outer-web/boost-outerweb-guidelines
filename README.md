# Boost Outerweb Guidelines

## Installation

Install the package via Composer:

```bash
composer require outerweb/boost-outerweb-guidelines --dev
```

Then install the guidelines with Boost:

```bash
php artisan boost:install
```